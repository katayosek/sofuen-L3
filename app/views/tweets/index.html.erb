<% if flash[:notice] %>
<%= flash[:notice] %>
<% end %>

<% @tweet.each do |e| %>
    <p>
        <%= e.message %>
        <%= e.tdate %>
        <%= link_to '表示', "/tweets/#{e.id}" %>
        <%= link_to '削除', "/tweets/#{e.id}", method: :delete %>
    </p>
<% end %>

<%= link_to 'ツイートする', '/tweets/new' %>